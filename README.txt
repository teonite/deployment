===========
TEONITE
Deployment and database migration tool
===========

Before use this script please configure config.ini from config.ini.template and logger.conf from logger.conf.tpl.
Those files are located in:
- virtualenv: <virtualenv root>/teonite/deployment
- linux: /usr/local/teonite/deployment

config.ini can also be provided by -c/--config switch